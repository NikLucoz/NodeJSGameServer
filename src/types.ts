export type playerData = {
    _id: number,
    pos: Array<number>,
    socket_id: string,
    points: number
}

export type globes = {
    pos: Array<number>
}

export type movementData = {
    key: string,
    _id: number,
}